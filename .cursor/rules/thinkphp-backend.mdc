---
description: 适用于基于 ThinkPHP 8 框架的后端 API 服务开发规范
globs: 
  - "app/**/*.php"
  - "route/**/*.php"
  - "config/**/*.php"
alwaysApply: false
---
# ThinkPHP 8 后端开发规范

本项目后端采用 ThinkPHP 8 框架提供 API 服务。

## 0. 环境与版本限制
- **PHP 版本限制**: 必须使用 **PHP 8.2** 的语法特性（如只读类、类型提示、null/false 联合类型等），但**绝对不能**使用 PHP 8.3 或更高版本的专属语法。
- **MySQL 版本限制**: 生产环境使用 **MySQL 5.7.44**。所有生成的 SQL 查询和表结构定义必须兼容 MySQL 5.7。**严禁**使用 MySQL 8.0+ 的专属特性（如 `window functions` 窗口函数、`CTE`、部分新的 JSON 函数等）。

## 1. 架构分层规范
- **Controller (控制器)**: 位于 `app/controller/`。仅负责接收请求参数、参数验证、调用 Service 层处理业务逻辑，以及返回统一格式的 JSON 响应。**禁止在 Controller 中写复杂的业务逻辑或直接操作数据库。**
- **Service (服务层)**: 位于 `app/service/`。负责所有的核心业务逻辑（如 AI 绘图请求、闯关跳级判定等）。
- **Model (模型层)**: 位于 `app/model/`。负责定义数据表关联和数据层面的通用操作。

## 2. 接口响应规范
所有接口均需返回统一格式的 JSON 数据，格式如下：
```json
{
  "code": 200, 
  "message": "success",
  "data": {} 
}
```
- `code: 200` 表示成功，非 200 表示业务异常或错误。
- `message` 为提示信息。
- `data` 存放实际返回的数据体。
- **注意**: 当使用 `ResponseHelper::success()` 返回成功响应时，它的参数顺序是 `($data, string $message)`，即**第一个参数是 data，第二个参数是 message**，切勿传反导致 500 强类型错误。

## 3. 鉴权与安全
- **JWT 鉴权**: 依赖微信 `code` 登录换取 JWT Token。
- **请求携带**: 客户端在 Header 中携带 `Authorization: Bearer <token>`。
- **中间件拦截**: 使用 `\app\middleware\Auth` 中间件拦截需要鉴权的路由。
- **用户信息获取**: Auth 中间件会将 `user_id` 和 `openid` 注入到 Request 对象中，控制器中必须使用 `$request->user_id` (下划线命名) 获取，**切勿使用** `$request->userId`。获取后应强制转换为整型 `(int) $request->user_id`，以避免强类型声明时的 500 错误。

## 4. 异常处理与强类型
- PHP 8.2 开启了强类型检查 (`declare(strict_types = 1);`)。
- 当接收外部参数传入 Service 层时，必须进行显式的类型转换（如 `(int) $request->param('page')`）。
- 所有可能抛出异常的 Service 调用，Controller 层必须使用 `try...catch` 包裹，并统一通过 `ResponseHelper::error()` 返回前端。

## 4. 谐音梗图游戏 - 中级 / 小红书专辑特殊逻辑
- **防跳关与进度一致**: `mid` / `xhs` 的 `level` 往往不连续。提交 `/pun/answer/submit` 时，服务端用与 `/pun/level/progress` 相同的规则计算当前应玩 `currentLevel`（`pun_levels_issue2` / `pun_levels_issue3` 有序前缀 + `passed_levels_*` + 排行榜兜底）；仅当提交的 `level` 等于该 `currentLevel` 且答案全对时，才更新 `max_level_mid` / `max_level_xhs` 与进度表，既防跳关，又避免进度表与排行榜不同步导致“答对却不推进”。

## 5. 定时任务
- 项目依赖宿主机或 Docker 的 crontab 执行内部接口（如 `curl http://localhost/cron/send-remind`）以派发久坐提醒。
- 定时任务相关逻辑封装在 `app/controller/Cron.php` 和对应的 Service 中。