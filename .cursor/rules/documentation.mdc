---
description: 项目文档编写与维护规范
globs: 
  - "docs/**/*.md"
  - "*.md"
alwaysApply: false
---
# 项目文档规范

## 1. 文档结构
- 项目所有的系统架构、API 接口和数据库表结构必须统一在 `docs/SYSTEM_DOC.md` 中进行维护。
- 禁止在项目中散落创建零碎的说明文档，所有相关说明均应合并入统一文档体系。

## 2. 数据库设计
- 项目当前的数据库定义文件应当存放于 `docs/database.sql`。
- 如果有任何数据库表结构的调整（新增表、修改字段），必须：
  1. 更新 `docs/database.sql` 里的语句。
  2. 同步更新 `docs/SYSTEM_DOC.md` 里的「二、 数据库核心表结构」板块。

## 3. 编写格式
- 文档需使用规范的 Markdown 语法。
- 接口文档必须包含：接口路径、请求方法、作用说明、是否需 Token、详细的请求体与响应体示例。